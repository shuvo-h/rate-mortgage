import React from 'react';
import Layout from '../components/Layout/layout';

const Notice_to_vendor = () => {
    return (
        <Layout className='container'>
            <h1>Notice to Vendors - Contract Authority</h1>
        </Layout>
    );
};

export default Notice_to_vendor;