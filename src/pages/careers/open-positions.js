import React from 'react';
import Layout from '../../components/Layout/layout';

const Open_positions = () => {
    return (
        <Layout className='container'>
            <h1>Guaranteed Rate jobs</h1>
        </Layout>
    );
};

export default Open_positions;